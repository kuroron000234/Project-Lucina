{
  "name": "Monica VitalOS",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "pip install -e . && if [ ! -f .env ]; then cp .env.example .env 2>/dev/null || true; fi",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
